'use strict';

describe('sad.settings module', function() {

  beforeEach(module('sad.settings'));

  describe('settings controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('SettingsController');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});